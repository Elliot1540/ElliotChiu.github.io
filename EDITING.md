# 个人主页维护说明

本次沿用原有 Jekyll / AcademicPages 项目、酒红色主题和 GitHub Pages 路径。

## 三语内容

右上角可切换 English、简体中文、繁體中文。选择保存在当前浏览器中，并在主页、简历、项目与学术交流页面之间保持。JavaScript 不可用时保留完整英文页面。

`assets/js/translations.js` 管理界面与个人资料翻译。键为页面英文文本，值包含 `en`、`zh-Hans`、`zh-Hant`。修改英文正文时应同步修改对应翻译键。论文标题、期刊名、作者和文献引用保留原有书目文字，避免将译名误认为正式出版信息。

## 经历

主页 `_pages/about.md` 和简历 `_pages/cv.md` 已补充：

- 江南大学设计学院暑期学校：2023 年 7–8 月。
- 南京师范大学暑期学校：2023 年 6–7 月。
- 日本东北大学 RIEC 访问研究：计划于 2026 年 10–12 月开展。学术交流页也显示此计划。

访问研究结束后，请根据实际情况手动更新状态。

## 批量替换会议照片

1. 把 JPG、PNG 或 WebP 照片放到 `images/exchange/`，建议宽度约 1440 像素。
2. 编辑 `_data/exchange.json`：每个对象对应一张照片，顺序即轮播顺序。
3. 把 `image` 从空字符串改为 `/images/exchange/文件名.jpg`；填写三语 `title`、`caption` 和 `alt`（照片内容描述）。路径不需要加仓库名。
4. 复制条目即可增加照片，删除条目即可减少照片。主页和学术交流页会同步更新。

示例：

```json
{
  "image": "/images/exchange/conference-01.jpg",
  "title": {"en": "Conference name", "zh-Hans": "会议名称", "zh-Hant": "會議名稱"},
  "caption": {"en": "Date and location", "zh-Hans": "日期与地点", "zh-Hant": "日期與地點"},
  "alt": {"en": "Description of the photo", "zh-Hans": "照片内容描述", "zh-Hant": "照片內容描述"}
}
```

目前展示从用户提供材料中挑选的 14 张活动照片，涵盖 10 场活动。英文会议名为描述性翻译。轮播默认自动播放，支持按钮和左右方向键；每 4 秒切换一张，淡色进度条显示当前进度；可随时暂停或手动切换，鼠标悬停、切换到其他标签页时暂停，键盘聚焦时暂停播放。系统开启减少动态效果时不会自动轮播。图片完整显示，不裁切合照。手动切换会重置当前计时；滑出可见区域或切换浏览器标签时暂停计时。最新两场活动位于照片清单最前。

## 社会任职与会员

`_data/memberships.json` 保存原有四项会员身份及机构官网链接。官网链接只用于指向机构，不代表官网公开认证了个人会员身份。

## 项目详情页

项目详情页位于 `_portfolio/`。在页首配置中加入 `category`、`image` 和 `image_alt`，即可让项目卡片显示横版图片。详情页图片集中放在 `images/projects/`，主视觉建议使用 20:9 横图，并提供准确的替代文字和图注。

需要公开资助单位但不公开金额时，可将金额写为 `**`。新增加的页面文字、图注和图片替代文字均应在 `assets/js/translations.js` 中同步维护英文、简体中文与繁体中文。

## 发布

原有 `.github/workflows/pages.yml` 在 `master` 更新后构建并部署。保留 `_config.yml` 的 `baseurl`，除非另行配置自定义域名。没有修改域名或 DNS。

## 姓名与学历

中文姓名：赵季淳；繁体：趙季淳。学士为艺术学学士，专业为数字媒体艺术；硕士为艺术学硕士，专业为设计学。英文使用 Master’s/Bachelor’s Degree in Art 并注明专业，不推定未经核实的 MFA/BFA 缩写。
